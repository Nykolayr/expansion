# Кампания Classic: миссии 1–15 (дизайн)

> Статус: **утверждено** (2026-07-10). Реализация — по одной миссии + playtest.  
> **Публикация м6+:** только OTA на сервер — [`docs/campaign-content-ota.md`](../../docs/campaign-content-ota.md).  
> Арт: канон имён в `BattleVisualId` / `BattleAssets.targetAssetPaths`; промпты для ИИ — отдельным блоком позже.

## Параметры захватываемых баз

| Параметр | Назначение |
|----------|------------|
| `speedBuild` | Скорость роста ракет |
| `speedResources` | Доход ресурсов на апгрейды |
| `shield` | Стартовый щит |
| `maxShips` | Порог автопостройки: при `ships >= maxShips` рост останавливается; подкрепления могут превышать лимит |
| `ships` | Стартовый гарнизон |

Код: `NeutralBaseBalance` (`lib/game_core/battle/neutral_base_balance.dart`).

### Профили по размеру

| Тип | `ships` | `maxShips` | `speedBuild` | `speedResources` | `shield` |
|-----|---------|----------|--------------|------------------|----------|
| Малая (`smallBase`) | **20** | **30** | 0.1 | 0.15 | 0 |
| Средняя (`middleBase`) | **40** | **50** | 0.1 | 0.15 | 0 |
| Большая (`base`) | **60** | **80** | 0.1 | 0.15 | 0 |

Базовые типы отличаются **только лимитом**. Спец-варианты (`rich`, `factory`, …) — другие пропорции и завышенные значения.

### Спец-варианты (`variant` в JSON)

| `variant` | Эффект | Visual id |
|-----------|--------|-----------|
| `rich` | `speedResources` ×5 | `base_rich` |
| `shielded` | щит 30 / 40 / 50 по размеру | `base_shielded` |
| `factory` | `speedBuild` ×1.4, `ships` ×0.7 | `base_factory` |
| `bunker` | `maxShips` ×1.4, `speedBuild` ×0.75 | `base_bunker` |

Матки (HQ): лимит 200, статы из `mainShipOur` / `mainShipEnemy`.

---

## Летающие объекты (hazard)

| Миссия вводит | Id | Статус кода |
|---------------|-----|-------------|
| 1 | `hazard_asteroid` | ✅ |
| 3 | `hazard_comet` | ✅ |
| 5 | `hazard_debris` | ✅ |
| 7 | `hazard_pulse` | ✅ |
| 9 | `hazard_drone` | ✅ |
| 11 | `hazard_mine` | ✅ |
| 13 | `hazard_solar_wind` | ✅ |
| 15 | `hazard_wormhole` | ✅ |

Конфиг: `BattleMissionHazards`.

---

## Миссии 1–15

| # | Имя | Карта | Базы | Новый hazard | Урок |
|---|-----|-------|------|--------------|------|
| 1 | Солнце | Коридор, HQ по центру | **Только малая** | Астероид | Туториал: захват, щит, апгрейд |
| 2 | Титавин | Фланг: игрок (1,7), враг (5,1) | Малая + **средняя** | — (астероид реже) | Средняя = лимит 50 vs 30 |
| 3 | Алараф | Два коридора, центр занят | Малая + средняя | **Комета** | Не слать флот в срез |
| 4 | Конопус | Острова слева/справа | + **большая** | — | Большую брать последней |
| 5 | Пылающая | Песочные часы (узкий центр) | + `variant: rich` | **Обломки** | Богатая база = ресурсы в тылу |
| 6 | Альциона | L-форма по углам | + `shielded` | — | Щит перед астероидом |
| 7 | Алиот | Шахматная сетка | + `factory` | **Импульс** | Фабрика быстро разгоняется |
| 8 | Электра | Кольцо по периметру | + `bunker` | — | Бункер держит линию |
| 9 | Сеат | Треугольник к врагу | 2× rich | **Дрон** | Дрон = бесплатные корабли |
| 10 | Интеркрус | Три пути наверх | больше средних | — | Выбор маршрута |
| 11 | Маркаб | Асимметрия, давление сверху | микс + shielded + rich | **Мина** | Разминирование |
| 12 | Алмак | Спираль к врагу | factory + bunker | — | Длинные линии снабжения |
| 13 | Мусцида | Две арки | плотный микс | **Солнечный ветер** | Тайминг отправки |
| 14 | Адара | Крест, пустой центр | все спец-варианты | — | Сводка баз |
| 15 | Алголь | Широкий фронт | полный набор | **Червоточина** | Короткий путь vs риск |

---

## JSON layout (`objects_N.json`)

- `typeNeutral`: `smallBase` | `midleBase` | `base` (legacy имена).
- `variant` (опционально): `rich` | `shielded` | `factory` | `bunker`.
- Переопределения (опционально): `ships`, `shild`, `maxShips`, `speedBuild`, `speedResources`.

При смене bundled-сцен — поднять `GameDatabaseConstants.bundledContentVersion`.

---

## Реализация (порядок)

1. ✅ Фундамент: `NeutralBaseBalance`, `BattleVisualId`, `BattleMissionHazards`
2. ✅ М1: только малые базы
3. ✅ М2: карта фланга + средние базы
4. ✅ М5: песочные часы + rich + обломки
5. ✅ М6–М15: раскладки + hazards (comet/pulse/drone/mine/solar/wormhole)
