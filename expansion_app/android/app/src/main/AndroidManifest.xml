<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Сеть: любые HTTP-клиенты, WebSocket и т.д. Должно быть в main — release не подмешивает src/debug. -->
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <!-- Камера/галерея и др. обычно подтягиваются merge из плагинов — дублировать без нужды не стоит. -->
    <application
        android:label="Expansion"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
    <queries>
        <!-- Flutter: ProcessTextPlugin -->
        <intent>
            <action
                android:name="android.intent.action.PROCESS_TEXT"/>
            <data
                android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher + видимость пакетов (API 30+): canLaunchUrl / внешний браузер -->
        <intent>
            <action
                android:name="android.intent.action.VIEW"/>
            <data
                android:scheme="https"/>
        </intent>
        <intent>
            <action
                android:name="android.intent.action.VIEW"/>
            <data
                android:scheme="http"/>
        </intent>
    </queries>
</manifest>