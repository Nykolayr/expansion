/// Ключи [FlutterSecureStorage] — только секреты, не prefs.
class SecureStorageKeys {
  SecureStorageKeys._();

  static const accessToken = 'auth_access_token';
  static const refreshToken = 'auth_refresh_token';
}
