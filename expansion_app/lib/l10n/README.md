ARB для `flutter gen-l10n`. Базовый язык шаблона — **ru** (`app_ru.arb`), для мультиязычных приложений — **en** (`app_en.arb`).

После изменения ARB выполни в корне проекта: `flutter gen-l10n`. В коде: `package:<name>/l10n/app_localizations.dart` (поле `name` из `pubspec.yaml`).
